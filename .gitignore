# Rust
/target/
**/*.rs.bk
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Secrets (though we don't store them in plaintext like some people)
*.pem
*.key
secrets/

# Build artifacts (but NOT audit logs — those are operational records)
build/
dist/
*.tmp.log
